# shooting_range
> A Vue.js project

### Build Setup
```angular2html
# install dependencies
npm install

# serve with hot reload at localhost:3000
npm run serve

# build for production with minification
npm run build
```
